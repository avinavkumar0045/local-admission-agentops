================================================================================
AgentOps Project Tracker
================================================================================
This file tracks all facts, updates, and major architectural decisions made during development.

[2026-08-15 12:38:00] INITIALIZATION
- Initialized core project directory structure (/src, /data, /ui).
- Defined Instrumentation-by-Design (Option 2) as the core architectural strategy.
- Selected tech stack: Qwen (via Ollama), FAISS/Neo4j, FastAPI, MySQL, Streamlit, Grafana.
- Verified local Qwen model (`qwen2.5-coder:latest`, 4.7 GB) is successfully installed on Apple Silicon (M2 Air) via Ollama.
- Generated requirements.txt with all core dependencies.
- Generated initial README.md.

[2026-08-15 13:05:00] PHASE 2 COMPLETION (KNOWLEDGE BASE)
- Created mock admission documents (eligibility.md, fees.md).
- Solved Python 3.13 / pip environment compatibility issues by resolving dependencies via Anaconda.
- Developed ingest.py to parse, chunk, embed, and store knowledge into FAISS.
- Embedded telemetry (AgentOps) hooks directly into the ingestion script (load_model, chunking, embedding, saving).
- Successfully executed ingestion: 4 chunks embedded with dimensionality 384.

[2026-08-15 13:35:00] PHASE 3 COMPLETION (BASELINE RAG AGENT)
- Adopted strict new coding guidelines (saved in `.agents/rules/coding_guidelines.md`). Added professional file documentation headers to all project files.
- Built `src/agent/retriever.py` to handle FAISS lookups.
- Built `src/agent/generator.py` to handle LLM generation via `httpx` to local Qwen. 
- Increased HTTP timeout to 120s to accommodate heavy local LLM inference on the Apple Silicon M2 chip.
- Built `src/agent/api.py` as a FastAPI orchestrator.
- Verified end-to-end functionality: The API successfully retrieved context and generated a highly accurate admission response while emitting all 3 AgentOps spans (retrieve_context, llm_generation, full_query_lifecycle).

[2026-08-15 14:15:00] PHASE 4 COMPLETION (AGENTOPS BACKEND)
- Set up local MySQL server via Homebrew on the host machine.
- Executed `schema.sql` to initialize the `agentops_db` schema (sessions, traces, spans).
- Updated `telemetry.py` to establish a persistent connection using `mysql-connector-python`.
- Converted the `emit_span()` function to execute dynamic `INSERT` queries instead of standard print statements.
- Verified telemetry persistence by testing `ingest.py` and querying the MySQL database directly, confirming successful logging of duration_ms and metadata.

[2026-08-15 14:30:00] PHASE 5 COMPLETION (STREAMLIT UI)
- Created `ui/styles.css` implementing a custom Glassmorphic Black theme (blur filters, glowing borders, radial gradients) for a highly premium aesthetic.
- Developed `ui/app.py` utilizing `st.chat_message` and `st.session_state` to render a ChatGPT-style conversational interface.
- Built Operational Transparency features: implemented a `st.expander` named "View Sources" that dynamically renders the FAISS chunks and their relevance distance underneath the AI's generated response.
- Orchestrated the final dual-server setup: The FastAPI backend on port 8000 (logging to MySQL) and the Streamlit frontend on port 8501.

[2026-08-15 14:45:00] PHASE 6 COMPLETION (GRAFANA OBSERVABILITY)
- Automatically installed Grafana 13.1.3 via Homebrew and started the background service on port 3000.
- Drafted exact UI instructions for the user to connect the `agentops_db` MySQL database to Grafana and write the SQL query for extracting `duration_ms` vs `span_type`.
- The AgentOps Local RAG project is now 100% complete and functionally verified end-to-end.
